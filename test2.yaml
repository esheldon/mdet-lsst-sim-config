sim:
    bands: [g, r, i]
    gal_type: wldeblend
    psf_type: gauss
    # coadd_dim: 301
    # se_dim: 301
    coadd_dim: 100
    se_dim: 100
    buff: 10
    # noise_factor: 0.5
    noise_factor: 0.3

mdet:
    meas_type: gap
    deblend: true
    # deblender: shredder
    deblender: scarlet
    shredder_config:
        psf_ngauss: 1
    stamp_size: 48
    detect:
        thresh: 5

coadd:
    nowarp: true
