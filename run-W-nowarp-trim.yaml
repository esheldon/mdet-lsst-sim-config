# default coadd_dim is now 250
# we trim 50 as we will do for real data

sim:
    gal_type: wldeblend
    # bring noise down to approximately 3 band level
    noise_factor: 0.58

coadd:
    nowarp: true

mdet:
    meas_type: wmom
    trim_pixels: 50
